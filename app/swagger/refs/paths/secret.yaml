get:
  description: Returns a cool secret
  operationId: secret
  tags:
    - Secret
  responses:
    200:
      description: Success results in successful secret-learning
      schema:
        $ref: '../models/secret.yaml'
    default:
      description: Default response
      schema:
        $ref: '../models/error.yaml'
